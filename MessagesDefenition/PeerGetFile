Message Received :
    PeerPseudo : RANDOMSTRING
    message : ENCRYPTED (with N1 pbk)
        senderPBK : INT
        NextNode : PORT (N2)
        message : ENCRYPTED (with N2 pbk)
            senderPBK : INT
            NextNode : PORT (NK-1)
            message : ENCRYPTED (with NK-1 pbk)
                ...
                senderPBK : INT
                NextNode : PORT (NK)
                message : ENCRYPTED (with NK pbk)
                    CommunicationType : ITTERCLASS
                    NextNode : TRACKER
                    FileName : String


==> WAIT FOR RESPONSE

Message To Send:
    message : ENCRYPTED (with SYM1 Pbk)
        SymKey : INT
        message : ENCRYPTED (with SYM2 pbk)
            SymKey : INT
            message : ENCRYPTED (with SYM3 pbk)
                ...
                SymKey : INT
                message : ENCRYPTED (with SYMk pbk)
                    SymKey4 : INT
                    message : ENCRYPTED (with Symkey4)
                        File : Binary


==> SAVE FILE BINARY TO ./FILENAME